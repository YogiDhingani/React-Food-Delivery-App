export type ProductModel = {
    id: string;
    name: string;
    description: string;
    price: number;
};

export type CartProductModel = {
    id: string;
    name: string;
    description: string;
    price: number;
    quantity: number;
};